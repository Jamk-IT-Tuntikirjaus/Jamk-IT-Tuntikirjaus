import { ElementRef, OnDestroy } from '@angular/core';
export declare class MdMenuNotClosable {
    private elementRef;
    constructor(elementRef: ElementRef);
    /**
     * contains
     * @param element
     */
    contains(element: HTMLElement): boolean;
}
export declare class MdMenu {
    private elementRef;
    private isVisible;
    notClosable: MdMenuNotClosable;
    constructor(elementRef: ElementRef);
    /**
     * open menu
     */
    open(): void;
    /**
     * close menu
     */
    close(): void;
    /**
     * check closeble
     * @param element
     */
    isInClosableZone(element: HTMLElement): boolean;
}
export declare class MdMenuOpen implements OnDestroy {
    private menu;
    private elementRef;
    private close;
    constructor(menu: MdMenu, elementRef: ElementRef);
    private open();
    ngOnDestroy(): void;
}
export declare const MD_MENU_DIRECTIVES: any[];
export declare class MdMenuModule {
}
